/** @type {import('next-i18next').UserConfig} */
module.exports = {
  i18n: {
    locales: ["en", "fr"], // Remplacez par vos locales
    defaultLocale: "fr", // Locale par défaut
    localeDetection: false, // Désactive la détection automatique
  },
};
